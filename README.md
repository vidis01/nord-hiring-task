# nord-hiring-task
## Main refactoring points:
* server_list command logic with parameters was implemented by using System.CommandLine;
* appsettings.json stores all API URLs;
* commands.json stores all commands with parameters;
* For easy code maintainability or future changes, repositories were used for fetching, storing, logging, and displaying data
* .NET version was not changed, so C# language capabilities were used accordingly
